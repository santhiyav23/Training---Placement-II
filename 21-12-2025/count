class Solution:
    def numRookCaptures(self, board: List[List[str]]) -> int:
        a=board
        for i in range(0,8):
            for j in range(0,8):
                if a[i][j]=='R':
                    m=i
                    n=j
        count=0
